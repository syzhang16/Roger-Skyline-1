maj packages : sudo apt update && sudo apt full-upgrade

creer un utilisateur : sudo useradd id

droits sudo : sudo usermod -aG sudo id

installer vim : sudo apt install -y vim

DHCP :
configuer dhcp avec ip statique et netmask /30 :
    - mettre la vm en bridge
    - trouver le gateway du mac : route get default | grep gateway
    - changer le fichier /etc/resolv.conf
    - restart la vm : 
    nameserver 10.12.17.140
    nameserver 8.8.8.8
    nameserver 8.8.4.4
    - modif fichier /etc/netplan/50-cloud-init.yaml
    # This file is generated from information provided by
    # the datasource.  Changes to it will not persist across an instance.
    # To disable cloud-init's network configuration capabilities, write a file
    # /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg with the following:
    # network: {config: disabled}
    network:
    version: 2
    renderer: networkd
    ethernets:
     enp0s3:
       dhcp4: no
       addresses: [10.12.17.140/30]
       gateway4: 10.12.254.254
       nameservers:
       addresses: [8.8.8.8,8.8.4.4]
       
    - appliquer : sudo netplan apply
